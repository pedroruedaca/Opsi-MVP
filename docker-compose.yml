services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: opsi
      POSTGRES_PASSWORD: opsi
      POSTGRES_DB: opsi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
